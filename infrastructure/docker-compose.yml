version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  ai-service:
    build:
      context: ../ai-service
    ports:
      - "8000:8000"
    volumes:
      - ../ai-service:/app
    environment:
      - PYTHONUNBUFFERED=1
